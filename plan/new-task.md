# Task: 前端&后端问题修复清单

> 每项含状态、位置、验收标准。Codex 可按优先级顺序逐项执行。

---

## ✅ 已完成

- [x] **T1 数据集切换** — `app.js` 数据加载入口，切换后地图&面板同步刷新
- [x] **T2 路线策略区分度** — 不同策略输出不同路线，已标注耗时
- [x] **T3 AIGC 动效展示** — 前端 AIGC 结果区，API 调用后地图/画布动态效果
- [x] **T5 地图节点搜索** — `app.js` 搜索事件绑定 + SearchService，输入节点名→定位+高亮
- [x] **T6 日记模块布局** — web 前端日记页 CSS Grid/Flex，固定 3 列等宽等高
- [x] **T7 图片去单一化** — 美食/设施图片不再使用单一占位图；本轮已按 `plan/*.png` 补齐 22 张美食演示图

---

## 🟡 P1 — 功能打磨

### T4 室内导航增强
- **状态:** ✅ 完成：路径摘要、楼层步骤、不可达提示、弹窗平面图、缩放/拖拽交互、室内/室外路线色彩区分
- **当前方案:** 弹窗/侧栏平面图（Popup Sidebar Floor Plan）+ 可交互平面图
- **位置:** `web/scripts/route-indoor.js` + `web/styles.css`
- **后续可做:**
  1. 多楼层切换动画
  2. 更真实的建筑平面图底图
- **验收:** 弹窗平面图可放大、缩小、重置和拖拽查看；室内路径高亮色与室外路线区分

---

## 🟢 P2 — 前端排版优化

### N1 路线时间显示整合
- **状态:** ✅ 完成
- **描述:** 路线耗时显示应与「生成游览路线」按钮放在同一区域，更直观
- **位置:** `web/index.html` + `web/scripts/route-indoor.js`
- **动作:**
  1. 将时间信息从结果列表移至按钮旁/下方
  2. 生成路线后时间就地刷新
- **验收:** 点击生成路线后，时间与按钮在同一视觉区域内展示

### N2 日记模块收折
- **状态:** ✅ 完成
- **描述:** 写日记表单占空间大，加一个切换按钮，点击再展开
- **位置:** `web/index.html` + `web/scripts/startup.js` + `web/styles.css`
- **动作:**
  1. 添加「写日记」toggle 按钮
  2. 默认收起表单区域
  3. 点击展开/收起，带过渡动画
- **验收:** 默认只显示按钮，点击后展开表单，再次点击收起

### N3 图片手动替换（演示优化）
- **状态:** ✅ 完成
- **描述:** 美食/设施图片已去单一化，部分可手动替换为更精美的演示图片
- **位置:** `web/assets/food/summer_palace/`、`web/assets/food/tsinghua_campus/`、`cpp/data/**/restaurants.json`、`web/data/**/restaurants.json`
- **方式:** `plan/*.png` 按餐厅名称复制到对应数据包资源目录，并同步更新 C++/Web JSON
- **验收:** `verify-t7-images.ps1` 断言 22 张 plan 美食图均被对应餐厅引用

### N4 首页探索模块动效
- **状态:** ✅ 完成
- **描述:** 首页探索模块展示图可添加动图元素或多背景轮播
- **位置:** `web/index.html` + `web/styles.css` 首页渲染区
- **动作:**
  1. 使用 4 张现有真实景点图
  2. CSS `hero-crossfade` 淡入淡出轮播
- **验收:** 首页探索区有动态视觉效果，非静态单图

---

## ⚪ P3 — 代码质量

### N5 app.js 拆分
- **状态:** ✅ 已拆分并保守收尾；进一步小文件化后续处理
- **位置:** `web/scripts/*.js`
- **动作:**
  1. 保留现有 `core/data/map-controller/route-indoor/recommend-search/diary-aigc/food/startup` 拆分
  2. `web/index.html` 按依赖顺序引入拆分脚本
  3. 不在本轮强行压缩所有脚本到 300 行以下，避免无收益回归
- **验收:** `verify-remaining-tasks.ps1` 确认不再加载单体 `app.js`，且拆分脚本齐全

### N6 JSON 解析器加固
- **状态:** ✅ 完成
- **位置:** C++ JSON 解析代码
- **动作:**
  1. 列出所有被解析的 JSON 字段
  2. 为每个正则解析点添加边界 case 注释
  3. 数据格式变动时同步更新
- **约束:** 不引入外部 JSON 库，不做全量重写
- **验收:** `cpp/include/tripsystem/utils.hpp` 与 `cpp/include/tripsystem/data_manager.hpp` 有注释说明覆盖范围和边界行为

---

## 执行顺序

```
N1 ✅ → N2 ✅ → T4 ✅ → N4 ✅ → N3 ✅ → N5 ✅ → N6 ✅
 ↑ 路线时间整合
    ↑ 日记收折
       ↑ 室内导航增强
          ↑ 首页动效
             ↑ 图片手动替换（可并行）
                ↑ 代码拆分
                   ↑ 解析器加固
```
