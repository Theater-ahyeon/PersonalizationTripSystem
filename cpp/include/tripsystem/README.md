# tripsystem include directory

This directory is reserved for public C++ headers as the project grows. The current implementation keeps the first runnable version in `cpp/src/main.cpp` so the course demo can build with one command.
