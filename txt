必须优化的点：
1.对于vagabond大标题这一块可以改个和旅游系统相符合的名称并加个适当设计个logo
2.在探索推荐的模块可以展示几条每个景点的评论
3.颐和园的设施名称有些太笼统了，需要改一下，比如知春亭地铁站3，可以叫知春亭地铁站A出口，颐和园东宫门卫生间1直接就叫颐和园东宫门卫生间
4.日记模块使用aigc的生成模拟分镜动画，可以开始接入api了，目前考虑的时千问的可以吗还是有什么别的推荐。
5.目前已经能够支持用户登录以及多用户切换，但是太简陋了，并且用户与用户之间没有隐私性，功能也没有实现完全分离，可以优化一下
6.添加的清华大学相关的设施美食和地点，都可参考颐和园的命名方式，避免太虚假。或者可以爬取真实数据来修改这两个地方的设施和美食的名称。
以下为可能需要，也可能是多此一举：
1.增加前端和 C++ 的一致性测试。
2.优化路线页布局，降低侧栏压迫感。
3.为大规模日记和目的地数据建立倒排索引、分页和缓存。
潜在关注点：
JSON 解析脆弱：基于正则的 jsonObjects/jsonString/jsonNumber 无法处理嵌套对象、转义引号。但控制的数据文件格式固定，不会触发
内存模型：所有数据存 vector<T> + HashMap 索引。删除操作会引起迭代器失效
Huffman 无删除接口：DataManager 提供 writeDiary 但无 deleteDiary，CLI 界面也只支持写入和浏览
Graph 边存储：使用 HashMap<int, vector<Road>>，每次 neighbors() 返回副本（值拷贝），大规模图有性能开销
Trie 内存泄漏：clear() 递归 delete 子节点，但析构函数 ~Trie() 调用 clear(&root_) 时不会删除 root_ 本身——不过 root_ 是栈对象，这是正确做法
前端 app.js 体积大：109KB 单文件，维护性随功能增加下降

