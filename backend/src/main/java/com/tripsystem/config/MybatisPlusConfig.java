package com.tripsystem.config;

import org.springframework.context.annotation.Configuration;

/**
 * 课程要求：核心功能不使用数据库。
 *
 * 该配置类仅作为项目配置占位，后续如需要做“非核心功能”的 DB 对比/扩展，再引入相应依赖与配置。
 */
@Configuration
public class MybatisPlusConfig {
}
